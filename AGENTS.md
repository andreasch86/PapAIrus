# Non negotiable guidelines for this code repository

* Test coverage should *ALWAYS* be 100%, no branching allowed.
* Always run the pre-commit tool before finishing and commiting work.
* If pre-commit fails, always implement fixes without changing the tests.